---
description: 새 기능을 대화로 구체화 → PRD → Jira 이슈 생성 (jira-prd 스킬)
---

`jira-prd` 스킬을 실행합니다. 한 줄 아이디어를 함께 논의(discovery)해 PRD 로 정리한 뒤 Jira 이슈로 마무리합니다.

- 요지가 명확해 바로 티켓만 만들면 되면 `/jira-new` 를 쓰세요.
- 절차·규칙은 `.claude/skills/jira-prd/SKILL.md` 를 따릅니다.

$ARGUMENTS
